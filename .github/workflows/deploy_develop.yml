name: Deploy Develop

on:
  push:
    branches:
      - develop

jobs:
  deploy_develop:
    name: Deploy Develop
    uses:
      tstelzle/DigitalCoinLibrary/.github/workflows/deploy.yml@develop
    with:
      environment-var: develop
      secrets_environment-var: development